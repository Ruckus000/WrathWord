export NODE_BINARY=/opt/homebrew/Cellar/node/24.1.0/bin/node
# Embed a JS bundle in Debug to ensure device builds always have a fallback
# and avoid “No script URL provided” when Metro is unreachable.
export DEV=false
